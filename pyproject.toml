[project]
name = "jev"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.3",
    "slack-bolt>=1.30.0",
    "typesafe-sdk>=0.6.0",
]
